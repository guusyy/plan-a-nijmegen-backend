{
  "routes": [
    {
      "method": "GET",
      "path": "/perks",
      "handler": "perks.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/perks/count",
      "handler": "perks.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/perks/:id",
      "handler": "perks.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/perks",
      "handler": "perks.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/perks/:id",
      "handler": "perks.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/perks/:id",
      "handler": "perks.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
